*.iml
.gradle/
/local.properties
.idea/
.DS_Store
build/
captures/
.externalNativeBuild/
.cxx/

# Generated by uniffi-bindgen from the Rust FFI crate -- not automated into a
# Gradle step yet (see the setup notes), copied in manually for now. The .so
# is a large (~200MB+ unstripped debug) build artifact, regenerated with:
#   cargo build -p isekai-client-ffi --target aarch64-linux-android
# See ~/isekai-link-android-camera-client-setup.md's "Gradle/Kotlin app
# project" section for the full recipe.
app/src/main/jniLibs/**/*.so
